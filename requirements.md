# 血圧記録アプリ 要件定義書

## 1. 目的

本アプリは、ユーザーがスマートフォンで日々の血圧を簡単に記録し、過去の記録を一覧およびグラフで確認できるようにすることを目的とする。

サーバーやAPIは使用せず、端末内のSQLiteデータベースにデータを保存する。
個人利用を前提とし、オフライン環境でも利用できるシンプルな血圧管理アプリとする。

---

## 2. 対象ユーザー

主な対象ユーザーは、日常的に血圧を記録したい個人ユーザーである。

想定ユーザーは以下の通りである。

* 毎日または定期的に血圧を測定している人
* 手書きやメモ帳ではなく、スマホで簡単に記録したい人
* 過去の血圧変化をグラフで確認したい人
* ネット接続がない環境でも記録したい人

---

## 3. システム構成

本アプリは、スマートフォン上で動作するモバイルアプリである。
データはスマートフォン内のSQLiteに保存する。

```text
スマホアプリ
  └ expo-sqlite
      └ スマホ内SQLiteデータベース
```

APIサーバー、クラウドDB、ログイン機能、複数端末同期機能は今回の対象外とする。

---

## 4. 利用技術

* Expo
* React Native
* TypeScript
* expo-sqlite
* グラフ表示ライブラリ

  * react-native-chart-kit または victory-native

---

## 5. 機能要件

### 5.1 血圧記録機能

ユーザーは血圧データを入力し、端末内に保存できる。

入力項目は以下とする。

| 項目   | 必須 | 内容        |
| ---- | -- | --------- |
| 上の血圧 | 必須 | 収縮期血圧     |
| 下の血圧 | 必須 | 拡張期血圧     |
| 脈拍   | 任意 | 1分あたりの脈拍数 |
| 時間帯  | 任意 | 朝、昼、夜     |
| メモ   | 任意 | 体調や状況のメモ  |
| 記録日時 | 自動 | 保存時の日時    |

---

### 5.2 入力チェック機能

ユーザーが不正な値を入力した場合、保存せずにエラーを表示する。

チェック内容は以下とする。

| 項目   | 条件              |
| ---- | --------------- |
| 上の血圧 | 必須、数値、正の整数      |
| 下の血圧 | 必須、数値、正の整数      |
| 脈拍   | 任意、入力された場合は正の整数 |
| メモ   | 任意、長すぎる場合は制限する  |

上の血圧が下の血圧より低い場合は、確認メッセージまたはエラーを表示する。

---

### 5.3 記録一覧表示機能

保存済みの血圧データを一覧表示できる。

一覧では以下の情報を表示する。

* 記録日時
* 上の血圧
* 下の血圧
* 脈拍
* 時間帯
* メモの一部

新しい記録が上に表示されるよう、記録日時の降順で並べる。

---

### 5.4 記録詳細表示機能

一覧から記録を選択すると、詳細情報を確認できる。

詳細画面では以下を表示する。

* 上の血圧
* 下の血圧
* 脈拍
* 時間帯
* メモ
* 記録日時

---

### 5.5 記録編集機能

保存済みの記録を編集できる。

編集可能な項目は以下とする。

* 上の血圧
* 下の血圧
* 脈拍
* 時間帯
* メモ

記録日時は原則として変更しない。

---

### 5.6 記録削除機能

保存済みの記録を削除できる。

誤削除を防ぐため、削除前に確認メッセージを表示する。

---

### 5.7 グラフ表示機能

保存済みの血圧データをグラフで表示できる。

グラフでは以下を表示する。

* 上の血圧の推移
* 下の血圧の推移

可能であれば、脈拍の推移も表示対象とする。

表示期間は初期段階では直近7件または直近30件とする。

---

### 5.8 データ保存機能

入力された血圧データは、端末内のSQLiteに保存する。

アプリを閉じてもデータは保持される。

---

## 6. 非機能要件

### 6.1 操作性

* 入力項目はできるだけ少なくし、短時間で記録できるようにする
* 必須項目は上の血圧と下の血圧のみとする
* スマートフォンで片手操作しやすい画面構成にする

---

### 6.2 性能

* 記録一覧はスムーズに表示されること
* グラフ表示は数十件から数百件程度のデータで問題なく動作すること

---

### 6.3 可用性

* インターネット接続がなくても利用できること
* アプリを再起動しても保存済みデータを閲覧できること

---

### 6.4 保守性

* 入力画面、一覧画面、グラフ画面、DB処理を分離して実装する
* 将来的にAPI化しやすいよう、データ操作処理は専用ファイルにまとめる

---

## 7. 今回の対象外

今回の開発では、以下は対象外とする。

* ログイン機能
* ユーザー登録機能
* APIサーバー
* クラウドDB
* 複数端末同期
* 医師や家族との共有機能
* PDF出力
* 通知機能
* バックアップ機能

ただし、将来的な拡張候補としては残す。

---

## 8. 将来的な拡張候補

今後、必要に応じて以下の機能を追加する。

* APIサーバー連携
* クラウドDB保存
* 複数端末同期
* ログイン機能
* Googleアカウント連携
* CSV出力
* PDF出力
* 血圧の平均値表示
* 高血圧判定の目安表示
* 服薬記録
* 測定リマインダー通知

---

## 9. 完成条件

初期版の完成条件は以下とする。

* 血圧データを入力できる
* 入力データをSQLiteに保存できる
* 保存したデータを一覧表示できる
* 保存したデータを削除できる
* 血圧の推移をグラフ表示できる
* アプリを再起動してもデータが残っている
